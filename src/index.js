import React from "react";
import reactDom from "react-dom";
import ReactDOM from 'react-dom';
import {
  BrowserRouter as Router,
  Switch,
  Route,
  Link
} from "react-router-dom";

reactDom.render(
  <React.StrictMode>
    <Router>
      <Switch>
        <Route>
          
        </Route>
      </Switch>
    </Router>
  </React.StrictMode>
);